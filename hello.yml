config:
  target: https://www.artillery.io
  # Load the Playwright engine:
  engines:
    playwright: {}
  # Path to JavaScript file that defines Playwright test functions
  # TypeScript is also supported
  processor: './playwright.config.ts'
scenarios:
  - engine: playwright
    testFunction: helloWorld